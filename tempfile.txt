     <form onSubmit={handleSubmit}>
      {/* className='mt-10 w-full max-w-2xl flex flex-col gap-7' */}
        <textarea 
          // value={post.text}
          onChange = {(e) => setPost({...post, text: e.target.value})}
          placeholder='What do you think? Share it.'
          required
          className='form_textarea'
        />
        <div className="form_horizontal_line"></div>
        <div className="form_buttons_container">
          <button type="submit" className="submit_form_button" onClick={handleSubmit}>Post</button>
        </div>

      </form>
